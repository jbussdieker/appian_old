<%= render 'header' %>

<div class="container">
  <code>git clone git@localhost:<%= current_user.name %>/<%= @repository.name %></code>
</div>

<br>

<% if @repository.description != "" %>
<p class="well">
  <%= @repository.description %>
</p>
<% end %>

<% if @commit %>
<div class="well">
  Latest commit to the <%= @branch %> branch
  <div>
    <%= @commit.message %>
  </div>
  <div>
    <%= @commit.author.name %> (<%= @commit.author.email %>)
  </div>
</div>
<br>
<% end %>

<%= render 'breadcrumbs' %>
<%= render 'files' %>
<%= render 'readme' %>

